(* 練習問題 3.5 *)

(*
let x = e1 and y = e2;;
let x = e1 let y = e2;;

上記2種類の宣言の違いは, e2式が同行で宣言されている変数xのスコープに含まれるか否かである.
1行目の宣言ではe2がxのスコープに含まれ, 2行目の宣言では含まれない.
現時点では言及されていないが rec を使うときに相互再帰をするために and で宣言する必要あり

*)